# an application for moving brick

### environment
    python 3.6

### installment
    pip install pipenv
    pipenv --three
    pipenv install

### run
    pipenv shell
    python main.py {tradePlace} {dataType}
    ex. python main.py MaxMaiCoin Tick